transceiver qsfp default-mode 4x10G

hostname {{ hostname }}

spanning-tree mode mstp

aaa authorization exec default local

aaa root secret 0 {{ passwd }}

interface Ethernet1

interface Ethernet2

interface Ethernet3

interface Management1
   ip address {{ mgmt_ip }}

no ip routing

management api http-commands
   no shutdown

end
